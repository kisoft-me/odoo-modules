<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Inherited form view of account move -->
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.view.form.inherit.sales.contract.and.recurring.invoices</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="contract_origin" invisible="1"/>
            </xpath>
        </field>
    </record>
    <record id="view_sale_form" model="ir.ui.view">
        <field name="name">sale.order.view.form.inherit.sales.contract.and.recurring.invoices</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/list/field[@name='price_unit']" position="after">
                <field name="contract_id" invisible="1" optional="hide"/>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="contract_id" readonly="1" string="Contract Reference"/>
            </xpath>
        </field>
    </record>
</odoo>
